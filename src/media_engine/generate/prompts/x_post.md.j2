Here are the user-facing changes (experience summaries) synthesized from the
project's recent development activity:

{% for s in summaries %}
{{ loop.index }}. {{ s.headline }}
   Before: {{ s.before }}
   After: {{ s.after }}
   Experience: {{ s.experience }}
{% if s.supporting_detail %}
   Behind it (supporting only): {{ s.supporting_detail }}
{% endif %}
{% endfor %}

---

Write {{ count }} X (Twitter) post candidates drawn from these changes.

Rules:
- Write in {{ language }}.
- Lead with WHAT CHANGED for the audience, in this priority order:
    1. the value the audience gains (the "after")
    2. the problem it removes (the "before")
    3. the experience it unlocks
    4. implementation detail — ONLY as a closing aside, never the lead
- The before -> after contrast is the strongest device; use it. Write "what
  changed for the user", not "what I implemented".
- Do NOT spread one post per change. Prioritize the highest-value changes; it is
  fine to write several posts (different angles) for a strong one and to skip
  weak ones entirely.
- Never invent capabilities beyond what the changes state.
- Each post stands alone and must fit within 280 characters INCLUDING hashtags.
- Put 1-3 hashtags inline at the end of the post text (do not add them twice):
  {{ hashtags | join(" ") }}
- No code blocks. No markdown. Plain post text.

Return ONLY a JSON array, no prose, with this exact shape:
[
  {"angle": "<short label>", "text": "<the full post text, hashtags included>", "hashtags": ["#..."]}
]
