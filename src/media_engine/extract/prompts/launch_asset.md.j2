Here is recent development activity for the project (commit messages and diffs,
already redacted).

Period: {{ period_start }} to {{ period_end }}

Development activity:
{{ material }}

---

Synthesize this activity into a SMALL number (2-5) of "launch assets" — the units
actually worth communicating to users.

Rules:
- Do NOT create one asset per commit. GROUP related commits into a single theme.
- Only keep themes that have real communication value. DROP purely internal
  changes (refactors, retries, flags, config) UNLESS they translate into
  user-facing value such as trust, safety, or stability.
- Express each asset as USER VALUE — what the user can now do and why it matters —
  not as WHAT was implemented.

Examples of the grouping intended:
- several security/hardening fixes -> "You can now use it with confidence"
- OAuth retry + secure cookie + HTTPS -> "Stable usage while out and about"
- mobile UI fixes -> "Phone monitoring is now practical"

Return ONLY a JSON array, no prose, with this exact shape:
[
  {
    "headline": "<short value-oriented theme title>",
    "what_achieved": "<the user-facing value, 1-2 sentences>",
    "why_built": "<why it matters / the pain it removes, 1 sentence>",
    "source_refs": ["<sha>", "..."]
  }
]
